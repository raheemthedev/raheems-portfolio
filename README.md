# Raheem Portfolio

A responsive portfolio landing page based on the supplied UI reference.

Open `index.html` directly in a browser, or serve the folder locally:

```sh
python3 -m http.server 4173
```

Project titles, disciplines, location, and contact information are placeholders
and can be edited in `index.html`.
